{
  "name": "lambda-example",
  "version": "1.0.0",
  "description": "Serverless deploy example",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local": "NODE_ENV=local nodemon --inspect src/server.js --presets env,stage-2",
    "deploy:dev": "NODE_ENV=development sls deploy --stage dev",
    "deploy:prod": "NODE_ENV=production sls deploy --stage prod"
  },
  "author": "begginer",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "mysql2": "^1.5.3",
    "sequelize": "^4.37.6",
    "serverless-http": "^1.5.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.25.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-2": "^6.24.1",
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.11.0"
  }
}
